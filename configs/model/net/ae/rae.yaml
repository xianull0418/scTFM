# @package _global_

# RAE (Regularized Autoencoder)
# 本质上使用 Vanilla AE 架构，配合 L2 正则化 Loss (在 AELitModule 中处理)

# 覆盖 AELitModule 的参数
model:
  reg_type: l2
  reg_weight: 1e-4

# 使用基础的 Vanilla 网络结构
defaults:
  - /model/net/ae: vanilla
  - _self_

